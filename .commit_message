chore: update Android config, dependencies, and documentation

This commit includes configuration fixes, dependency updates, and
documentation improvements to align with CHANGELOG.

Fixed:
- Fixed Android package name in pubspec.yaml to match plugin structure
  * Changed from com.example.flutter_network_watcher
  * Changed to com.github.dhia_bechattoui.flutter_network_watcher

Changed:
- Updated connectivity_plus dependency from ^6.0.0 to ^7.0.0
- Removed pubspec.lock files from version control (package best practice)
- Updated .gitignore to exclude lock files

Documentation:
- Updated README platform support section to match CHANGELOG details
  * iOS: Added CocoaPods + Swift Package Manager mention
  * Web: Updated to "Full support with WASM compatibility"
  * macOS: Added Swift implementation + SPM details
  * Windows/Linux: Added C++ implementation details

This resolves Android compilation issues and aligns documentation with
the actual platform support capabilities documented in the CHANGELOG.

Fixes #1
